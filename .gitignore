# Local overlays and machine-specific state
*.local
# ...but keep the XDG `.local/` dirs that stow packages live under.
!**/.local/
local/

# Editor and OS noise
.DS_Store
Thumbs.db
*~
*.bak
*.tmp

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
